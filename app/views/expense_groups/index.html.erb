<p style="color: green"><%= notice %></p>

<h1>Transactions</h1>

<div id="expenses">
  <% @expenses.each do |expense_group| %>    
      <%= render expense_group %>      
  <% end %>
</div>


<%= button_to "ADD NEW TRANSACTION", new_group_expense_group_path(group_id: @group.id), method: :get, class: 'splash-btn green' %>
